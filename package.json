{
  "name": "concierge-collector",
  "version": "3.0.0",
  "description": "Restaurant curation and collection tool for concierges",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:ui": "vitest --ui"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^1.6.1",
    "@vitest/ui": "^1.0.4",
    "dotenv": "^17.2.3",
    "fake-indexeddb": "^5.0.2",
    "jsdom": "^23.2.0",
    "vitest": "^1.0.4"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
